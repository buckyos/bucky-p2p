# Fallback Rendezvous Failures to Legacy SnCall Acceptance Report

## Findings
| ID | Severity | Owning Stage | Correctness Category | Evidence | Problem | Blocking |
|----|----------|--------------|----------------------|----------|---------|----------|
| F-054-A1-001 closed | none | none | test-adequacy | The contradictory `rendezvous_sn_absent_deterministic_failure_skips_local_action_and_probes_once` no longer exists under `p2p-frame`; the retained dedicated regressions are at `tunnel_manager_tests.rs:522-788`; the latest formal DV runs `tunnel::tunnel_manager::tests` and exits zero | Iteration one found a task-042 inline test that required zero legacy action and was omitted by the task runner. The superseded test was removed after a fixture-corrected red observation, and the complete x509-enabled TunnelManager test surface now covers the resulting source state. | no |

## Prior Finding Closure
| Prior ID | Closure Evidence | Status |
|----------|------------------|--------|
| F-054-A1-001 | Repository search finds no retained stale test symbol; `tunnel_manager.rs:7067-7113` now ends with only the unchanged failure-classification unit test; `testplan.yaml` replaces the narrow NAT-aware DV with `cargo test -p p2p-frame --features x509 --lib tunnel::tunnel_manager::tests -- --test-threads=1`; `.harness/test-results/test-runs/20260904T071026Z-p2p-frame+054-fallback-rendezvous-to-legacy-all.json` records that DV command and both focused commands with exit code 0, with 82 TunnelManager tests reported passed by the fresh run | closed |

## Residual Diagnostic Boundary
| Diagnostic | Direct Source and Failure Boundary | Relationship to CHG-fallback-rendezvous-to-legacy | Disposition |
|------------|------------------------------------|----------------------------------------------------|-------------|
| `sn::tests::sn_client_query_registered_peer_returns_full_info` | `p2p-frame/src/sn/tests.rs:589-606` calls `SNClientService::query` and fails because `SnQueryResp.end_point_array` is empty | No `TunnelManager` open method is called; the assertion concerns SN report/query fixture endpoint publication, before any NAT-aware fallback orchestration | unrelated, non-blocking diagnostic; not acceptance evidence |
| `sn::tests::sn_client_server_connection_covers_report_query_call_and_called_response` | `p2p-frame/src/sn/tests.rs:764-794` fails on the callee query's missing QUIC endpoint before its later direct `SNClientService::call` section | No `open_tunnel_from_id`, `open_tunnel_from_lookup`, or `open_nat_aware_tunnel` call exists in the test; the changed rendezvous-error branch is unreachable | unrelated, non-blocking diagnostic; not acceptance evidence |
| `sn::tests::sn_same_sn_tests::same_sn_rendezvous_response_owner_accepts_observed_ip_with_predicted_port` | `sn_same_sn_tests.rs:355-365` panics because `SnServer::get_peer_observed_ep` returns no observation, before listener installation or `rendezvous_via_sn` | The failure is in serving-SN observation fixture setup and occurs before even the direct SN rendezvous request; it does not enter TunnelManager or its changed fallback branch | unrelated, non-blocking diagnostic; not acceptance evidence |
| `sn::tests::sn_same_sn_tests::same_sn_rendezvous_response_owner_rejects_mixed_unowned_prediction` | `sn_same_sn_tests.rs:404-414` hits the same missing-observation precondition before constructing its direct SN rendezvous response path | The failure precedes `rendezvous_via_sn` and has no caller path to `TunnelManager::open_nat_aware_tunnel` | unrelated, non-blocking diagnostic; not acceptance evidence |

The diagnostic whole-library run in `.harness/test-results/test-runs/20260904T070902Z-p2p-frame+054-fallback-rendezvous-to-legacy-all.json` exited 101 with these four failures after 463 other tests passed. It is explicitly excluded from passing evidence. This acceptance does not claim the entire `p2p-frame` library suite is green.

## Object and Scope
- Task manifest: `task.yaml`
- Review date: 2026-09-04
- Review iteration: 2, performed fresh after the testing return by a reviewer that did not implement or test the change
- In-scope implementation: `DefaultDeviceFinder::get_peer_info`; `TunnelManager::open_tunnel_from_id`, `open_tunnel_from_lookup`, `open_nat_aware_tunnel`, `open_rendezvous_tunnel`, `open_nat_aware_tunnel_legacy`, `execute_nat_action`, and `open_proxy_path`; `SNClientService::call_via_sn`; dedicated NAT-aware tests; current proposal, pipeline plan, risk profile, testplan, runtime state, admission stamp, prior report, and both 20260904T070902Z/20260904T071026Z artifacts
- Review mode: independent falsification; the current sources and latest evidence were re-read, the four diagnostic failures were traced separately, and no previous conclusion was adopted

## Admission Audit
| Binding | Current Evidence | Status |
|---------|------------------|--------|
| approved proposal | Current `proposal.md` SHA-256 is `922aeb79f538da98e231394b6babc1768023e9d4ed24f7b160fba62684d4626c`, matching the admission stamp | pass |
| automatic design source | Current `pipeline/plan.md` SHA-256 is `332536b3b3af9225fb4656104be4b4879a6f9d5a7892accdbc1b4e6e233b43fe`, matching the admission stamp | pass |
| task identity and scope | Stamp binds version `v0.1`, module/target `p2p-frame`, submodule `054-fallback-rendezvous-to-legacy`, change ID `CHG-fallback-rendezvous-to-legacy`, and the two task scope paths | pass |
| reuse decision | Proposal/plan hashes and admitted scope are unchanged, so acceptance audits the existing valid stamp rather than replaying admission | pass |

## Requirement Coverage
| change_id | Requirement or Boundary | Source | Implementation Evidence | Finding | Status |
|-----------|-------------------------|--------|-------------------------|---------|--------|
| CHG-fallback-rendezvous-to-legacy | Every final rendezvous request/action error enters the existing real legacy `SnCall` orchestration with the original lookup inputs; PN follows only a failed legacy caller action; one lookup, existing failure classification, and one absolute deadline remain; regression evidence must cover the changed behavior | `proposal.md` P-001, Scope, Success Criteria, and Risks; `pipeline/plan.md` Acceptance Baseline, State Ownership, and Failure Flows | `DefaultDeviceFinder::get_peer_info` performs the sole `query_with_context` at `device_finder.rs:93-152`; `open_tunnel_from_lookup` passes that snapshot at `tunnel_manager.rs:773-818`; `open_nat_aware_tunnel` retains the original endpoint/name/context bundle and calls legacy on the awaited rendezvous error at lines 1857-1935; legacy allocates a fresh id, invokes `call_via_sn`, runs the caller action, and gives the same id to PN at lines 1938-2045; the outer timeout encloses all stages; the three dedicated tests and complete 82-test TunnelManager DV are green in the latest formal artifact | F-054-A1-001 is closed; no missing approved behavior or remaining task-scoped validation defect was found | pass |

## Independent Defect Discovery
| Category | Applicable Scope | Evidence Inspected | Adversarial Check | Finding or Not-Applicable Reason | Status |
|----------|------------------|--------------------|-------------------|----------------------------------|--------|
| requirement-and-behavior | P-001 fallback ordering, task-042 compatibility reversal, task-034 behavior, and explicit non-goals | current approved proposal; pipeline Acceptance Baseline and Rejected Alternatives; task 034/042 proposals; current production branch | Searched for ambiguity between the one peer query and restored second SN command, retained direct-PN behavior, errors excluded from fallback, and expansion into wire/plan/retry policy | The approved requirement explicitly supersedes task 042's zero-second-command choice while retaining one `query_with_context`; every returned final rendezvous error now enters legacy and no non-goal is implemented | pass |
| logic-and-control-flow | lookup-to-rendezvous-to-legacy-to-PN path and all match outcomes | `device_finder.rs:93-152`; `tunnel_manager.rs:745-818`, `1418-1667`, and `1857-2045`; `nat_connect_plan.rs:72-141` | Followed no-plan, rendezvous success, request failure, ambiguous internal retry success/failure, response/action failure, legacy SN-call failure, legacy action success/failure, and PN failure | A rendezvous plan yields a non-Legacy caller action; a successful rendezvous returns directly, any returned error reaches the legacy helper, and PN is reachable only from the post-driver legacy action-error arm | pass |
| boundary-and-input | original/filtered endpoint vectors, base/predicted candidates, absent SN/PN, derived identity/name/SN/context, and exhausted remaining time | cloning/move boundary at `tunnel_manager.rs:1895-1923`; request filtering at lines 1440-1476; legacy derivation at lines 1938-2004 and 2048-2115; dedicated tests at `tunnel_manager_tests.rs:522-788` | Challenged LAN-only endpoint loss, rendezvous-filtered values leaking into legacy, stale substitutes, plan-without-rendezvous behavior, missing services, and zero remaining budget | Only the rendezvous copy is consumed by the first attempt; legacy receives the complete original vector and the same other lookup-derived values. Missing PN remains a final error after legacy and an exhausted outer budget does not renew | pass |
| state-and-data-integrity | rendezvous owner/waiter, independent legacy id/waiter, registration, and retry side effects | owner token/registration helpers; `tunnel_manager.rs:1491-1666` and `1938-2045`; PN regression assertions; full TunnelManager DV | Tried stale owner cleanup against legacy, accidental rendezvous-id reuse, PN id divergence, partial registration, and duplicate action effects after ambiguous errors | Rendezvous completion/drop is generation-bound and finishes before legacy registration; legacy generates a new id and passes that exact variable to both action and PN. Preserved ambiguous local retry can precede legacy, but remains the explicitly unchanged classification behavior under one deadline | pass |
| error-handling-and-recovery | deterministic/ambiguous rendezvous errors, call/action errors, PN failure, timeout, and cancellation | `is_ambiguous_rendezvous_failure`; `open_rendezvous_tunnel`; `drive_nat_rendezvous_and_action`; legacy and proxy branches; focused deadline test | Checked swallowed errors, SN-call failure incorrectly forcing PN, caller success lost after call failure, premature PN, proxy absence, cancellation, and timeout remapping | Existing legacy semantics preserve caller-action success regardless of call failure; only caller-action error invokes PN; PN error is returned; outer expiration consistently returns the established total-deadline timeout | pass |
| resource-lifetime-and-cleanup | named lock, rendezvous registration/waiter, legacy registration, call/action futures, and timeout cancellation | RAII Drop implementations at `tunnel_manager.rs:165-243` and `363-417`; awaited rendezvous scope; legacy scope; full TunnelManager lifecycle tests | Searched for reentrant lock deadlock, owner/waiter survival into legacy, detached work, and retained resources after success/error/timeout | The rendezvous future's stack and named-lock guard are released on return before legacy acquires its lock; token-bound cleanup cannot remove the new legacy entry; outer cancellation drops the active future tree and the changed branch spawns no detached task | pass |
| concurrency-and-ordering | collision/winner completion, lock transition, concurrent legacy call/action, and sequential PN fallback | owner/cancel selects at `tunnel_manager.rs:1491-1666`; `drive_nat_rendezvous_and_action:98-114`; legacy lines 1952-2045; complete TunnelManager DV | Challenged collision completion races, legacy starting before cleanup, call/action coordination, PN racing the action, and waiter direction interference | Legacy begins only after awaited rendezvous completion and guard drop; existing call/action concurrency is unchanged; PN sits strictly inside `Err(action_err)` after the driver returns; the widened DV covers neighboring owner/waiter tests | pass |
| interface-and-compatibility | private helper/caller contracts, SN/PN command APIs, public opens, and historical behavior | pipeline Exported Interfaces/Consumer Migration Closure; caller searches; `call_via_sn`; `open_proxy_path`; task 034/042 baselines | Checked signatures, exports, wire encoding, serving-SN choice, second peer query, retry classification, public behavior, and unrelated consumers | The production edit is a private call-site restoration. It introduces the approved legacy SN command after failure but no new query, signature, export, wire field, plan selection, or failure classification; task 042's direct-PN behavior is intentionally replaced | pass |
| security-and-capacity | added legacy command/action work, endpoint trust, candidate count, deadline amplification, and allocations | proposal non-goals; outer timeout at `tunnel_manager.rs:1879-1935`; unchanged call construction at `sn_service.rs:1675-1817`; candidate caps and existing authentication | Challenged unbounded retries, renewed budgets, endpoint expansion, unauthenticated input, amplification, and new retained state | At most one existing legacy attempt is added after final rendezvous failure, inside the same absolute timeout and existing candidate limits; no trust decision, parser, externally supplied field, loop, or unbounded state is added | pass |
| test-adequacy | direct success without PN, direct failure then PN, shared deadline, stale expectation detection, neighboring lifecycle behavior, provenance, and diagnostic gaps | three tests at `tunnel_manager_tests.rs:522-788`; corrected `testplan.yaml`; 20260904T071026Z formal artifact; 20260904T070902Z diagnostic artifact; four failing SN/query tests and their setup paths | Asked whether tests can pass if rendezvous is skipped, legacy is skipped, PN is immediate, original endpoints are lost, deadline renews, old task-042 assertions survive, ids diverge, or lower-level unrelated fixtures fail | Focused cases expose legacy success/no-PN, legacy-before-PN, and deadline renewal; current source plus complete 82-test TunnelManager DV closes the stale-test and neighboring lifecycle surface. The four whole-lib failures occur without a call to the changed branch and are excluded from evidence; no zero-test or `cyfs-p2p-test` result is credited | pass |

## Document Consistency
| Document | Source | Implementation Consistency | Finding | Status |
|----------|--------|----------------------------|---------|--------|
| proposal | `proposal.md` | Current code and tests satisfy P-001, retain the named deadline/query boundaries, and make no public-NAT or wire claim | No requirement contradiction; task 042's old behavior is explicitly superseded | pass |
| design | `pipeline/plan.md` | Input ownership, post-rendezvous cleanup, new legacy id, legacy-only PN transition, failure flows, compatibility choice, and implementation scope match current code | No design-to-code mismatch found | pass |
| testing | `testplan.yaml`, runtime testing coverage, and 20260904T071026Z artifact | Focused commands match the three retained tests; DV now covers all TunnelManager tests and exits zero; testplan accurately excludes the four unrelated whole-lib failures and prohibits `cyfs-p2p-test` evidence | F-054-A1-001 is closed and the latest formal evidence matches the current source/testplan inputs; the whole-library diagnostic remains explicitly non-passing and out of the acceptance claim | pass |

## Result Summary
- Overall result: accepted
- Outcome: Rendezvous final failure now enters the existing legacy `SnCall` plus caller-action flow with the original lookup snapshot; a viable legacy direct path succeeds without PN, and only a failed legacy action reaches PN with the legacy id, all under one absolute deadline
- Prior blocking issue: F-054-A1-001 is closed by removal of the superseded inline test and a fresh complete TunnelManager DV run
- Residual validation boundary: the diagnostic whole-library run is not green because four SN report/query observation fixtures fail outside this fallback call path; it is not used as evidence. Local controlled tests also do not prove public-NAT, deployed multi-SN, real-router traversal, or every scheduler interleaving
- Blocking issues: none
- Next action: parent orchestrator may record accepted final state, complete the auto-pipeline lifecycle, and close unfinished-task bookkeeping

## Conclusion
- Accepted / rejected / needs changes: accepted
- Reason: fresh iteration-two falsification found the production ordering, input reuse, lock/owner transition, legacy/PN id ownership, and total deadline consistent with the approved requirement; the prior test defect is closed by current source and fresh full-TunnelManager evidence, and the four excluded whole-library failures have no call or assertion path to the changed branch
