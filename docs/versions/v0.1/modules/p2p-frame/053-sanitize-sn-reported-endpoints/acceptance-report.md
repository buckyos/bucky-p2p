# Sanitize SN-Reported Endpoints Acceptance Report

## Findings
| ID | Severity | Owning Stage | Correctness Category | Evidence | Problem | Blocking |
|----|----------|--------------|----------------------|----------|---------|----------|
| F-053-A2-000 | none | none | overall | Current production call chain; `rendezvous_initiator_ownership_requires_the_exact_current_command_tunnel`; positive and negative production-TTP cross-SN cases; eight-step `20260904T070000Z` task run | Iteration-two falsification found no remaining requirement, design, implementation, or validation defect in the approved scope. | no |

## Prior Finding Closure
| Prior ID | Closure Evidence | Status |
|----------|------------------|--------|
| F-053-A1-001 | `distributed_nat_profile_tests.rs:99-227` builds a certificate and peer-cache entry containing the same third-party public endpoint, proves empty requests remain allowed, proves non-empty requests fail with no exact tunnel, installs a live same-peer in-memory `CmdTunnel`, accepts only its actual id and observed IP, rejects that observed IP with a wrong id, rejects the cached/certificate IP on the actual live tunnel, and waits for tunnel cleanup; the exact test is registered and exits 0 in the fresh task run. | closed; the case fails both certificate/cache authorization and peer-observation-union substitutes while preserving exact-IP/different-port behavior |
| F-053-A1-002 | `testplan.yaml` now registers `cross-sn-positive-routing-integration`; `collision_cross_sn.rs:668-723` performs a production owner-directory query plus empty WaitIncoming from SN A through TTP to target SN B and asserts a successful response; the fresh task artifact records exit code 0 and 48.444 seconds for that exact command. | closed; positive and forged negative production inter-SN routes are both task-scoped and executed |

## Object and Scope
- Task manifest: task.yaml
- Review date: 2026-09-04
- In-scope implementation: `p2p-frame/src/sn/service/service.rs` report sanitization, exact request-tunnel authorization, peer-cache update ordering, same-SN delivery and source-SN relay ordering; task-owned unit/DV/integration tests and `testplan.yaml`; neighboring task-052 response-owner path
- Review mode: fresh iteration-two independent falsification from current proposal, design mapping, production callers, peer-cache owner, command-server tunnel lifecycle, repaired test sources, and runtime artifact before adopting prior findings or completion claims

## Requirement Coverage
| change_id | Requirement or Boundary | Source | Implementation Evidence | Finding | Status |
|-----------|-------------------------|--------|-------------------------|---------|--------|
| CHG-sanitize-sn-reported-endpoints | Retain only server-classified LAN and exact reporting-tunnel-IP public IPv4 endpoints; never authorize from report cache, certificate endpoints, area labels, or NAT profile; authorize every non-empty request against the exact current peer+tunnel observation before local/remote action; preserve empty WaitIncoming and existing structural/routing behavior | `proposal.md` Scope, P-001, Success Criteria, and Risks; `pipeline/plan.md` Acceptance Baseline, State Ownership, and Failure Flows | `SnService::sanitize_reported_endpoints` at `service.rs:594-629`; `handle_report_sn` ordering at `service.rs:1541-1624`; `get_peer_tunnel_remote` at `service.rs:1443-1454`; `rendezvous_endpoints_owned_by` at `service.rs:849-864`; `handle_rendezvous` before `process_rendezvous_request` at `service.rs:1079-1136`; peer-cache replacement under one mutex in `peer_manager.rs:164-186`; exact-tunnel/certificate test at `distributed_nat_profile_tests.rs:99-227`; same-/cross-SN socket tests | Production behavior and repaired validation cover the approved retention, authorization, ordering, compatibility, and routing boundaries; no missing approved behavior or implementation bypass was found. | pass |

## Independent Defect Discovery
| Category | Applicable Scope | Evidence Inspected | Adversarial Check | Finding or Not-Applicable Reason | Status |
|----------|------------------|--------------------|-------------------|----------------------------------|--------|
| requirement-and-behavior | P-001 retention, request authorization, empty-request compatibility, routing, and explicit per-port/legacy exclusions | approved `proposal.md`; `risk-profile.yaml`; `pipeline/plan.md`; current sanitizer, request handler, protocol validator, peer cache, and target tunnel manager consumer | Tried to reinterpret “observed IP” as cached, certificate-declared, area-declared, NAT-profile-derived, any-peer-tunnel, exact socket, or per-port proof and compared each interpretation with the approved review/non-goals | The approved baseline is internally coherent: exact live report/request tunnel IP is the authority, ports remain deliberately unproven, LAN values remain discovery hints, and task 052 plus legacy SnCall are excluded. No requirement ambiguity or missing production behavior was found. | pass |
| logic-and-control-flow | report admission and every rendezvous branch leading to cached, local, or relayed action | `sanitize_reported_endpoints`, `handle_report_sn`, `rendezvous_endpoints_owned_by`, `handle_rendezvous`, `process_rendezvous_request`, `deliver_rendezvous_to_local_peer`, and `relay_rendezvous_to_serving_sn` | Enumerated count 0/32/33, retained/omitted endpoints, empty/non-empty/mixed requests, missing exact lookup, local target, remote target, cached replay, and target error/timeout paths | Count greater than 32 returns before classification/cache/scheduler mutation; request ownership uses `all` and fails the whole mixed list; the ownership check precedes notify construction, state `begin`, local callback, and inter-SN lease lookup/relay. Empty structurally valid WaitIncoming continues past the ownership helper. No wrong branch or fallthrough was found. | pass |
| boundary-and-input | client-controlled endpoint count, protocol, socket IP family/class, port, area, duplicates, request shape, and encoded body | `MAX_REPORTED_LOCAL_ENDPOINTS`; sanitizer match arms; `Endpoint` equality; `is_non_lan_ipv4_addr`; `SnTunnelRendezvous::validate` and `MAX_SN_TUNNEL_RENDEZVOUS_ENDPOINTS`; sanitizer unit source | Challenged empty and over-limit vectors, exact 32 boundary by code condition, TCP/QUIC/Ext, RFC1918/link-local/ULA/IPv6 link-local, loopback/unspecified/multicast/broadcast/global IPv6, unrelated/exact public IPv4, zero port, forged areas, normalized duplicates, mixed transports, and duplicate rendezvous entries | The report sanitizer accepts only TCP/QUIC nonzero endpoints, derives LAN/Wan area from server evidence, excludes invalid/global-unowned classes, and deduplicates normalized protocol/socket tuples. Protocol validation independently preserves the rendezvous count, transport, area, address, port, and duplicate rules. No boundary defect was found. | pass |
| state-and-data-integrity | sanitized peer-cache snapshot, NAT-profile scheduler, repeated reports, over-budget failure, rendezvous dedup cache, and stale observations | `handle_report_sn:1587-1624`; `PeerManager::add_or_update_peer`; `reported_endpoints_for_peer`; `handle_query_sn`; `RendezvousState::begin/cache_response`; same-SN over-budget and mixed-list tests | Tried partial cache replacement on invalid members, atomicity loss on 33 entries, certificate/local endpoint recombination, stale cached response replay after address change, and NAT profile as alternate authority | Sanitization completes before the single peer-manager mutex update; invalid members are omitted from the new snapshot and over-budget input leaves the old cache intact. Certificate and NAT data may still serve their documented metadata/discovery roles but are not read by request ownership. Every replay re-enters ownership before rendezvous-state lookup, so a cached response cannot bypass a changed request-tunnel IP. | pass |
| error-handling-and-recovery | invalid report, absent observation, unauthorized/malformed rendezvous, target error, timeout, and relay fallback | `handle_report_sn` error propagation through the ReportSn command handler; `handle_rendezvous` failure closure; `process_rendezvous_request` timeout/error branches; DV assertions for generic client failure and preserved cache | Followed over-budget decoding to command failure, unsupported individual entries to omission, missing exact observation to fail-closed response, target callback rejection, local target miss to relay, and unauthorized requests to ensure no fallback action occurs | Over-budget reports fail before state mutation; unsupported members are deliberately sanitized rather than causing partial unsafe retention; unauthorized rendezvous returns the existing generic failure without local or remote fallback. Existing target and relay errors remain generic and bounded. No recovery defect was found. | pass |
| resource-lifetime-and-cleanup | endpoint vectors, command-tunnel snapshots, target QA task, timeout waiter, and cache/state locks | `get_peer_tunnel_remote`; `sfo-cmd-server` peer-manager connection Arc/removal lifecycle; `deliver_rendezvous_to_local_peer`; `RendezvousLeaderGuard`; peer-manager mutex scopes | Checked report/request rejection for retained handles, lock-held awaits, spawned target work before authorization, waiter leakage, and tunnel removal during handler execution | The sanitizer owns only bounded temporary vectors, locks are released without await, and unauthorized requests return before target QA task or rendezvous leader creation. The command handler's connection remains peer-bound while its receive task runs; an Arc snapshot may outlive map removal but preserves the authenticated remote value rather than creating a new authority. No new cleanup defect was found. | pass |
| concurrency-and-ordering | exact peer+tunnel lookup, concurrent tunnel add/remove, report/cache replacement, authorization/state-cache order, and local/remote delivery | `sfo-cmd-server` `PeerManager::find_connections/remove_peer_connection`; `get_peer_tunnel_remote`; `handle_report_sn`; `handle_rendezvous`; `RendezvousState` call order; exact-tunnel test cleanup at `distributed_nat_profile_tests.rs:211-226` | Tried alternate same-peer tunnel observations, disconnect between validation and lookup/action, concurrent reports, request replay, and TOCTOU between observed endpoint capture and target action | Production lookup filters by both handler-supplied peer and connection id, which is generated/stored by the command server and not client supplied. Capturing the endpoint before a concurrent close cannot change it into a third-party IP, ownership runs before rendezvous-state synchronization, and the repaired test distinguishes a live same-peer union from the exact handler tunnel then observes connection removal. No exploitable TOCTOU or alternate-tunnel bypass was found. | pass |
| interface-and-compatibility | private SN service helpers, ReportSn wire semantics, Query/SnCall consumers, rendezvous request validation/routing, and task-052 response ownership | `pipeline/plan.md` Exported Interfaces and API Impact; current helper/caller search; `reported_endpoints_for_peer`; protocol structs; `validate_rendezvous_response_owner`; focused task-052 response-owner run; positive production-TTP task step | Checked for public signature/wire changes, certificate removal, map-port/NAT-profile loss, exact-port restriction, operation/area relaxation, task-052 call removal, and same-/cross-SN semantic changes | Only private service interfaces changed; encoded certificates remain intact, ports are intentionally not proven, protocol validation is unchanged, and task-052 response validation remains called after target response. The correct-feature task-052 tests remain green, while the fresh task run now proves empty WaitIncoming still traverses the production SN A to SN B route. | pass |
| security-and-capacity | authenticated report trust boundary, initiator-IP authorization, reflection/amplification, cache/body limits, and shared-NAT limitation | ReportSn command registration/authentication; sanitizer; exact tunnel remote lookup; rendezvous validator and handler; `SnCmdPkgLen = U16`; forged-report socket tests; exact-tunnel/certificate unit test | Replayed report-then-request, certificate/cache declaration, forged area, NAT profile, mixed lists, alternate/stale tunnel, missing tunnel, same-IP different port, 33-entry report, 8-entry request, and shared-NAT counterexamples | Current production code closes the arbitrary-IP bypass: no self-asserted cache/certificate/profile source reaches authorization, every non-empty list is exact-tunnel-IP bound, and work retained after decode is bounded by 32/8 endpoints. The repaired test now fails both certificate/cache and peer-observation-union substitutes. Same-IP other-port/shared-NAT exposure remains the approved non-goal. | pass |
| test-adequacy | normal, boundary, negative, error, lifecycle, compatibility, cross-module, and security regression evidence | sanitizer and exact-tunnel unit cases; four same-SN DV sources; positive and negative production-TTP cross-SN sources; current `testplan.yaml`; `20260904T070000Z-p2p-frame+053-sanitize-sn-reported-endpoints-all.json`; task-052 focused run | Mapped each proposal/risk required check to an executable assertion; checked whether each test would fail for the old reported-IP union, a remaining certificate/cache union, a same-peer observation union, unconditional fail-closed relay, cache partial mutation, mixed-list partial action, and zero-test filters | Both prior gaps are closed. Eight registered commands map to concrete test functions, have positive durations, and exit 0: they cover sanitizer domains, exact current/missing/wrong tunnel ids, certificate/cache exclusion, the original authenticated ReportSn exploit, cache atomicity, same-SN positive/mixed/empty behavior, and production inter-SN positive/negative behavior. Evidence remains local loopback with a dirty worktree and fixture-seeded routing, so it does not claim public-NAT or deployed multi-SN validation. | pass |

## Document Consistency
| Document | Source | Implementation Consistency | Finding | Status |
|----------|--------|----------------------------|---------|--------|
| design | `pipeline/plan.md` | Compared acceptance baseline, private interfaces, state owners, failure flows, rejected alternatives, scope binding, and implementation sequence with current service code | Production implementation follows the exact report/request tunnel observation design, atomic report capacity failure, server-side endpoint classification, cache replacement, and pre-delivery authorization shape. | pass |
| testing | `testplan.yaml` | Compared every registered case and assertion with proposal Success Evidence, risk-profile required checks, exact filter names, current test sources, and the fresh eight-step task artifact | The repaired plan directly covers certificate/cache rejection, correct/wrong/missing exact tunnel ids and cleanup, and both positive and negative production inter-SN routes in addition to the original sanitizer, cache, same-SN, and forged-report coverage. | pass |

## Result Summary
- Overall result: accepted
- Outcome: the P1 report-then-rendezvous arbitrary third-party IP reflection path is closed by sanitized cache input plus exact current request-tunnel authorization, and iteration-two evidence now exposes both prior vulnerable substitutes while preserving same-/cross-SN success behavior
- Blocking issues: none found; F-053-A1-001 and F-053-A1-002 are closed by the repaired unit and task-scoped integration evidence
- Next action: parent orchestrator may record accepted final acceptance and complete the auto-pipeline exit conditions and task closeout

## Conclusion
- Accepted / rejected / needs changes: accepted
- Reason: fresh iteration-two falsification found the report capacity/cache ordering, server-side endpoint classification, exact peer+tunnel authorization, pre-delivery enforcement, compatibility paths, and repaired adversarial tests mutually consistent, with no remaining blocking issue in the approved scope
