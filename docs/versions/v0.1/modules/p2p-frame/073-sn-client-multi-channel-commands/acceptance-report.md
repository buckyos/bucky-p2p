# 073-sn-client-multi-channel-commands Acceptance Report

## Findings
| id | severity | owning_stage | correctness_category | evidence | problem | blocking |
|----|----------|--------------|----------------------|----------|---------|----------|
| F-073-0 | none | none | overall | current `p2p-frame/src/sn/client/sn_service.rs:807-860` and `:873-879`; current `p2p-frame/src/sn/service/service.rs:886-981`; `p2p-frame/src/sn/tests.rs:1246-1296`; task run `.harness/test-results/test-runs/20260913T083951Z-p2p-frame+073-sn-client-multi-channel-commands-all.json` | No open requirement, design, implementation, or testing defect found in the current delivery. The earlier blocking finding F-073-1 (an unreachable SN could never be evicted or re-registered) and the dependent F-073-2 coverage gap were re-derived from HEAD-vs-delivered behaviour and re-verified after the fix rather than adopted from the previous report. | no |

## Object and Scope
- Task manifest: `task.yaml`
- Version: v0.1
- Packet module: p2p-frame
- Review mode: independent acceptance review of the delivered SN multi-channel command change. The reviewer re-read the approved proposal and design, the current production sources, the `sfo-cmd-server` / `sfo-pool` contracts, the testing document, the delivered tests and the task test-run artifact; the previous acceptance conclusion was not trusted until the current evidence was inspected.
- Change ids under review: CHG-sn-client-classified-command-channel, CHG-sn-command-active-identity-lifecycle, CHG-sn-server-command-stream-selection, CHG-qa-timeout-command-stream-close, CHG-remove-active-sn-conn-id
- Delivered surface: `p2p-frame/src/sn/client/sn_service.rs`, `p2p-frame/src/sn/service/service.rs`, test surfaces under `p2p-frame/src/sn/tests.rs` and `p2p-frame/tests/`

## Requirement Coverage
| change_id | requirement_or_boundary | source | implementation_evidence | finding | status |
|-----------|-------------------------|--------|-------------------------|---------|--------|
| CHG-sn-client-classified-command-channel | every SN-client-initiated command uses a pool-selected classified stream, reusing idle streams and creating only below the configured cap | proposal P-001 success evidence | `SNClientService::send_sn_qa` acquires through `get_send_by_classified` (`sn_service.rs:807-860`); report, call, query and rendezvous all dispatch through it (`sn_service.rs:983`, `:1875`, `:2074`, `:2152`); `sfo-pool-0.2.8/src/worker_pool.rs:197-247` waits at `max_count` instead of failing | none | pass |
| CHG-sn-command-active-identity-lifecycle | active-SN acceptance and lifecycle updates are keyed by authenticated peer identity with per-request validation retained | design State and Ownership and Invariants | `update_active_sn` matches on `sn_peer_id` (`sn_service.rs:111-128`); `active_sn_matches` uses peer identity (`sn_service.rs:864-871`); `on_rendezvous_notify` ignores the incoming stream id and validates peer identity (`sn_service.rs:873-902`); report/call/query still validate `seq` and `sn_peer_id` (`sn_service.rs:1898-1906`, `:2110-2114`, `:2185-2189`) | none | pass |
| CHG-sn-server-command-stream-selection | server-initiated rendezvous QA distributes across the target's existing command streams and never creates client streams | proposal P-003 success evidence | `deliver_rendezvous_to_local_peer` reads `get_peer_tunnels`, returns NotConnected when the list is empty, rotates `notify_stream_cursor`, and sends on the selected `conn_id` with `send_by_specify_tunnel_with_resp` (`service.rs:886-981`, cursor at `service.rs:213`) | none | pass |
| CHG-qa-timeout-command-stream-close | a timed-out QA closes exactly its command stream and leaves the bearer and other streams usable | proposal P-004 success evidence | `send_sn_qa` calls `set_disable()` on the borrowed guard for QA failures (`sn_service.rs:838`, `:850`); `sfo-cmd-server-0.4.0/src/client/classified_client.rs:140-143` aborts that stream's receive task; a deadline failure keeps the active SN (`p2p-frame/src/sn/tests.rs:1126-1183`) | none | pass |
| CHG-remove-active-sn-conn-id | the public pinned stream handle is removed, consumers migrate, and only a SN that cannot provide any command stream loses its registration | proposal P-005 success evidence; design State and Ownership eviction rule | `ActiveSN` no longer declares `conn_id` (`sn_service.rs:163-175`); `remove_active_sn` (`sn_service.rs:873-879`) is called only from the `send_sn_qa` acquisition-failure branch (`sn_service.rs:816-830`); compiler-backed external positive/negative fixtures, removed-symbol scan and compile closure pass | none | pass |

## Independent Defect Discovery
| category | applicable_scope | evidence_inspected | adversarial_check | finding_or_not_applicable_reason | status |
|----------|------------------|--------------------|-------------------|----------------------------------|--------|
| requirement-and-behavior | all five change ids and their success evidence | `proposal.md` Proposal Items and Success Criteria; `design.md` Overall Approach, State and Ownership, Consumer Migration Closure; current delivered diff | searched for delivered behaviour outside the approved non-goals (wire change, unbounded streams, application-level waiter map) and for any required behaviour without implementation | Every proposal item maps to delivered code and evidence; the eviction/recovery transition the design now names explicitly is implemented and the timeout-retention rule is preserved. | pass |
| logic-and-control-flow | `send_sn_qa`, `remove_active_sn`, `deliver_rendezvous_to_local_peer`, registration scan | `sn_service.rs:807-879`; `service.rs:886-981`; `sn_service.rs:1114-1200`, `:1453-1475` | traced loop termination (bounded by `streams.len()`), cursor modulo, `last_err` fallback, the acquisition-vs-request failure split, and the `retain` predicate | Eviction fires only on acquisition failure; the request-failure path disables the stream and returns without eviction; the server loop always terminates and surfaces the last error. | pass |
| boundary-and-input | classification key, empty candidate list, peer mismatch, malformed response, closed endpoint | `sn_service.rs:807-846`; `service.rs:886-910`; `p2p-frame/src/sn/tests.rs:1246-1296` | forced empty stream list (NotConnected), foreign peer id (guard disabled + InvalidData), malformed bodies (codec error), and a real closed loopback port (connection failure) | Boundaries behave as designed; the closed-endpoint case is covered by a regression test that also observes automatic re-registration. | pass |
| state-and-data-integrity | active-SN records, probe registration generation, `wait_online` signal | `sn_service.rs:111-128`, `:873-879`, `:1114-1120`, `:1467`, `:1660-1680`; `p2p-frame/src/sn/tests.rs:1246-1296` | re-ran the earlier falsification attempt: enumerate every mutation of `active_sn_list` after an unreachable-SN failure and check what `wait_online` reports | The unreachable record is now removed, so `wait_online` and `ping_proc` no longer report online against a dead endpoint; the peer-keyed `retain` cannot disturb other SN records. | pass |
| error-handling-and-recovery | QA acquisition failure, QA timeout, transport error, SN unreachable | `sn_service.rs:807-879`; `p2p-frame/src/sn/tests.rs:1126-1296`; HEAD `sn_service.rs:1852-1862`, `:2063-2073`, `:2145-2153` | re-derived the HEAD-vs-delivered comparison, then re-ran the new test with eviction temporarily disabled to confirm it fails (observed assertion failure at `p2p-frame/src/sn/tests.rs:1276`) and passes with the fix | Recovery is restored: the unusable registration is evicted and the ping loop re-registers the serving SN from the configured SN list within one backoff interval; per-request timeouts still keep a reachable SN. | pass |
| resource-lifetime-and-cleanup | command-stream guards, disabled streams, server peer connections, evicted records | `sn_service.rs:838`, `:850`; `service.rs:940-981`; `sfo-cmd-server-0.4.0/src/client/classified_client.rs:482-494` | checked that a failed QA drops its guard (Drop calls `set_disable`), that the server shuts down only the selected stream half, and that eviction leaves no dangling waiter or timer | No leak found: the dependency aborts the selected stream's receive task, the server connection disappears when its receive loop ends, and eviction only drops owned profile state. | pass |
| concurrency-and-ordering | concurrent QAs per SN classification, notify cursor, eviction under concurrent commands | `sn_service.rs:917-925`; `sfo-pool-0.2.8/src/worker_pool.rs:197-247`; `p2p-frame/src/sn/tests.rs:1126-1183` | checked that pool acquisition waits rather than failing at the cap, that the cursor is a single atomic fetch-add, and that concurrent slow call/query keep separate streams | No race or ordering defect found; eviction is a single locked `retain` and cannot be triggered by capacity pressure. | pass |
| interface-and-compatibility | SN wire/command codes, `ActiveSN` public surface, `sfo-cmd-server` contract | `p2p-frame/tests/tunnel_rendezvous_protocol.rs`; `p2p-frame/tests/sn_protocol_version_public_api.rs`; `p2p-frame/tests/sn_active_sn_api_check.py`; `p2p-frame/tests/sn_protocol_real_network.rs` | ran the compiler-backed external positive/negative fixtures, the removed-symbol scan, the compile closure and the real protocol matrix | The only interface change is the intended breaking field removal, verified in both compiler directions; wire codes and payload shapes are unchanged. | pass |
| security-and-capacity | authenticated peer binding, notification spoofing, stream amplification | `sn_service.rs:864-902`; `service.rs:886-981`; `p2p-frame/tests/tunnel_rendezvous/sn_same_sn_tests.rs` | checked that server notify selection never creates target streams, that stream count stays bounded by the client's configured cap, and that a forged or unowned rendezvous cannot arm a target | No new capability or amplification path; inbound commands remain bound to the authenticated peer and eviction grants no additional authority. | pass |
| test-adequacy | task tests, contract checks and the unified run artifact | `.harness/test-results/test-runs/20260913T083951Z-p2p-frame+073-sn-client-multi-channel-commands-all.json`; `p2p-frame/src/sn/tests.rs:1246-1296`; `testing.md` Unit/DV/Case-Type tables | mapped each delivered branch to a case and re-checked the previously uncovered recovery direction with a red-green run | Coverage now includes unreachable-SN eviction plus automatic re-registration, the retention direction for timeouts and stream replacement, and the breaking-API contract set; 508 lib tests pass. | pass |

## Document Consistency
| document | source | implementation_consistency | finding | status |
|----------|--------|----------------------------|---------|--------|
| design | `design.md`, `risk-profile.yaml` | the design names the acquisition-failure eviction rule, the peer-keyed ownership model, the command-server liveness statement and the corrected consumer-migration row; the implementation follows each of them | no mismatch | pass |
| testing | `testing.md`, `testplan.yaml` | the testing document records the new recovery test, the red-green evidence and the 508-test lib suite, and the plan maps every `change_id` to contract/unit/DV/integration steps executed in the recorded task run | no mismatch | pass |

## Result Summary
- Overall result: accepted
- Outcome: SN client commands now use the classified command-stream pool, active-SN identity is peer-keyed with per-request validation, server-initiated rendezvous QA round-robins over the target's existing streams, a QA timeout closes exactly its stream, the public `ActiveSN.conn_id` field is removed with compiler-verified consumer closure, and a SN that cannot provide any command stream is evicted so the client re-registers instead of reporting online against a dead endpoint.
- Blocking issues: none
- Next action: complete the acceptance lifecycle receipt, remove the task from the unfinished index, and report to the user the resolved acceptance finding plus the remaining boundary that the classified pool's internal cap/eviction policy is consumed rather than re-verified here.

## Conclusion
- Accepted / rejected / needs changes: accepted
- Reason: the five approved change ids are implemented, covered and re-verified against current sources; the earlier blocking defect was fixed with red-green regression evidence, no blocking finding remains, and the design and testing documents match the delivered behaviour. Public NAT, multi-host and deployed SN environments remain outside this local evidence boundary.
