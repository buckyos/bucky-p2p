# Clear Proxy Upgrade State After Tunnel Close Acceptance Report

## Findings
| ID | Severity | Owning Stage | Correctness Category | Evidence | Problem | Blocking |
|----|----------|--------------|----------------------|----------|---------|----------|
| F-046-A1-000 | none | none | overall | Approved P-001; current registration, pruning, failure, and success-finalizer control flow; ten dedicated lifecycle tests; admission bindings; and the 20260903T104959Z task-run artifact | Fresh iteration-three falsification found no remaining requirement, design, implementation, or validation defect in the approved scope | no |

## Prior Finding Closure
| Prior ID | Closure Evidence | Status |
|----------|------------------|--------|
| F-046-A1-001 | `register_tunnel:3157-3209`, both read-side prune paths at lines 590-697, `finalize_proxy_upgrade_success:2918-2956`, and cleanup at lines 3325-3347 all hold `tunnels` through the `state` decision in one `tunnels -> state` critical section | closed; no cleanup/register gap or reverse manager-lock acquisition remains |
| F-046-A1-002 | `finalize_proxy_upgrade_success:2918-2982` requires the exact returned Arc to remain registered, non-proxy, and available; rejected finalization reconciles current topology and accepted finalization detaches proxies atomically before closing them outside locks | closed under the approved topology-owned success rule; stale failed completion remains generation-checked at lines 2774-2790 |
| F-046-A1-003 | Dedicated tests `proxy_registration_holds_tunnel_lock_until_state_decision`, `unavailable_returned_direct_cannot_finalize_replacement_proxy_lifecycle`, `live_registered_direct_finalization_retires_proxy_lifecycle`, and `new_proxy_beside_live_direct_does_not_schedule_upgrade` directly exercise the original missing boundaries | closed; the latest task-scoped unit and DV commands both returned exit code 0 |
| F-046-A1-004 | Registration now first handles a genuinely available non-proxy, resets for a genuinely available newly registered proxy, and otherwise delegates to complete available-topology reconciliation at `tunnel_manager.rs:3175-3207` | closed; unavailable direct preserves a live published proxy generation and unavailable proxy creates no orphan generation |
| F-046-A1-005 | `proxy_upgrade_lifecycle_tests.rs:327-394` adds deterministic unavailable-direct and unavailable-proxy registration cases, both reached by the focused unit and broad DV filters in the 20260903T104959Z artifact | closed; the repaired boundary is directly asserted rather than inferred from timing or a passing build |

## Object and Scope
- Task manifest: task.yaml
- Review date: 2026-09-03
- In-scope implementation: `p2p-frame/src/tunnel/tunnel_manager.rs` proxy-upgrade generation, registration, pruning, failure completion, and success finalization; `p2p-frame/tests/unit/tunnel/proxy_upgrade_lifecycle_tests.rs`; adapted inline proxy-upgrade fixtures; task metadata, admission evidence, return records, and latest run evidence
- Review mode: fresh independent falsification from current primary sources after the second return cycle; all five prior findings and new counterexample schedules were evaluated before selecting the conclusion

## Requirement Coverage
| change_id | Requirement or Boundary | Source | Implementation Evidence | Finding | Status |
|-----------|-------------------------|--------|-------------------------|---------|--------|
| CHG-clear-proxy-upgrade-after-close | Remove upgrade state after the final unavailable candidate is pruned, retain tracking for a remaining published proxy, prevent stale failure rescheduling, and preserve successful live-direct behavior without public or retry-policy changes | `proposal.md` P-001, Scope, Success Criteria, and Risks; `pipeline/plan.md` Acceptance Baseline and Failure Flows | Missing/empty bucket reconciliation at `tunnel_manager.rs:590-697` and `2711-2748`; atomic registration topology at lines 3157-3209; cleanup at lines 3325-3353; generation-bound failure at lines 2761-2875; exact-live-Arc success finalization at lines 2918-2982 | No missing behavior or boundary mismatch found; final proxy removal clears state, remaining published proxy owns state, stale failure cannot touch a missing/replacement generation, and live direct topology retires proxy state | pass |

## Independent Defect Discovery
| Category | Applicable Scope | Evidence Inspected | Adversarial Check | Finding or Not-Applicable Reason | Status |
|----------|------------------|--------------------|-------------------|----------------------------------|--------|
| requirement-and-behavior | P-001 cleanup termination, retained-proxy ownership, stale failure, successful direct behavior, and explicit non-goals | approved proposal sections; revised pipeline acceptance baseline; current implementation and task evidence | Replayed final close under housekeeping and reads, retained proxy after direct loss, cleanup/re-registration before failure, old success after replacement, and unavailable registration | Each approved outcome has a current production path and direct assertion; no behavior extends into retry cadence, selection policy, public API, or SN/PN protocol | pass |
| logic-and-control-flow | registration topology branches, missing/empty pruning, due collection, generation failure, and success finalizer | `reconcile_proxy_upgrade_state:2711-2748`, `collect_due_proxy_upgrades:2761-2772`, `attempt_proxy_upgrade_generation:2840-2875`, `finalize_proxy_upgrade_success:2918-2982`, and `register_tunnel:3133-3248` | Enumerated live and unavailable proxy/direct candidates, mixed buckets, absent bucket, duplicate same/different Arc, stale generation, success, failure, and termination branches | Available non-proxy removes state; live new proxy resets generation only without live direct; all other registration cases reconcile the complete available topology; no wrong branch or progress defect remains | pass |
| boundary-and-input | absent and empty buckets, hidden and published candidates, live and closed forms, duplicates, multiple candidates, and generation exhaustion | read-side paths at lines 590-697; topology scan at lines 2717-2747; registration at lines 3157-3209; allocator at lines 297-303; dedicated boundary tests | Challenged no map entry, zero remaining candidates, published proxy plus unavailable direct, unavailable proxy alone, live direct plus new proxy, candidate-id collision, and `u64` exhaustion | Missing buckets remove stale state, unavailable candidates do not own topology decisions, duplicates reconcile existing topology, and checked monotonic allocation prevents ABA rather than wrapping | pass |
| state-and-data-integrity | candidate buckets, generation identity, in-progress flag, retry timestamps, replacement lifecycle, and proxy detach set | `ProxyUpgradeState` and `ManagerState:247-317`; every state mutation in reconcile, registration, failure, and finalizer; ten dedicated tests | Tried partial updates across locks, stale failure against replacement, obsolete returned Arc, current live direct, repeated proxy registration, and mixed topologies | Candidate/state decisions are atomic under one order, failure completion matches generation, and success commits only against current live topology; no orphan, stale mutation, or partial detach remains | pass |
| error-handling-and-recovery | lookup/dial failure, stale completion, rejected success, publish race, close failure, and retry recovery | `try_upgrade_proxy_tunnel:2878-2916`, failure path at lines 2774-2790 and 2864-2873, rejected finalizer reconciliation at lines 2945-2954, registration/publish paths, and close logging at lines 2961-2980 | Followed missing/mismatched failure generation, device lookup fallback, removed/closed returned direct, unavailable registration followed by publish/prune, and proxy close error | Failed stale work cannot reschedule; rejected success restores topology-derived state; publish/prune races are serialized by the candidate lock; close errors occur after committed state and are logged | pass |
| resource-lifetime-and-cleanup | tunnel candidates, retry state, scheduler wakeups, spawned attempts, detached proxies, and manager destruction | scheduler loop at lines 496-524; due spawning at lines 2805-2821; finalizer at lines 2918-2982; cleanup at lines 3325-3353; manager `Drop` | Checked last-candidate success/failure cleanup, remaining proxy, unavailable candidate, in-flight task after removal, lock-external close, and task abort on manager drop | Final removal deletes both bucket and state, no failed completion recreates it, notifications occur after manager locks release, and detached/held tunnels are closed without retaining lock ownership | pass |
| concurrency-and-ordering | `tunnels` RwLock, `state` Mutex, per-remote registration Locker, tunnel availability, async attempts, notifications, and close callbacks | repository-wide manager lock acquisition search; combined scopes at lines 590-697, 2918-2956, 3157-3209, and 3327-3347; barrier-based lock test | Searched for `state -> tunnels` cycles and replayed cleanup/register, read/register, proxy/direct registration, re-registration/failure, finalizer/new-proxy, and candidate closure schedules | Every combined manager acquisition is `tunnels -> state`; no await, notify, or close occurs under both; either serialized ordering yields a topology-consistent state and no lost wakeup or deadlock was found | pass |
| interface-and-compatibility | private helper surface, crate-internal consumers, public TunnelManager behavior, tunnel selection, retry constants, and SN/PN contracts | pipeline Exported Interfaces and Consumer Migration Closure; current helper/caller search; proposal non-goals; latest task compilation evidence | Checked old helper removal, every changed private caller, public exports, selection preference, retry progression, wire types, and neighboring-module expectations | All changed interfaces remain private, callers compile, adapted backoff fixtures preserve the original cadence, and no public, wire, selection, or migration surface changed | pass |
| security-and-capacity | remote-keyed scheduler allocation, retry/connect amplification, manager lock duration, generation allocation, and peer-controlled ids | state map and allocator at lines 288-317; due scheduling at lines 2750-2821; cleanup/reconcile and lock-external effects | Challenged orphan map growth, work after last close, unbounded task recreation, lock-held async operations, generation wrap, and new trust-boundary inputs | Cleanup and unavailable-topology registration prevent orphan work, one in-progress flag bounds each generation, no async work executes under manager locks, checked allocation prevents ABA, and security boundaries are unchanged | pass |
| test-adequacy | normal, boundary, negative, error, lifecycle, concurrency, compatibility, and task-run provenance | ten tests in `proxy_upgrade_lifecycle_tests.rs:80-394`; retained-proxy, success, failure, short-window, and backoff inline fixtures; updated `testplan.yaml`; 20260903T104959Z run JSON | Mapped each design transition and prior finding to an assertion; verified barrier coordination instead of sleep-only ordering and exact Arc/topology assertions; checked runner commands and exit codes | Focused tests can expose the original leak and all five acceptance findings; both x509-enabled unit and DV steps completed with exit code 0 and no cyfs-p2p-test evidence was used | pass |

## Document Consistency
| Document | Source | Implementation Consistency | Finding | Status |
|----------|--------|----------------------------|---------|--------|
| design | `pipeline/plan.md` | Compared state ownership, all six failure flows, rejected alternatives, scope binding, and file sequence with current lock scopes, topology decisions, and completion behavior | Implementation follows fixed `tunnels -> state` ownership, generation-bound failure, exact-live-topology success, missing-bucket reconciliation, and successful-direct compatibility | pass |
| testing | `testplan.yaml` | Compared atomic-registration, cleanup, retained-proxy, stale failure, success finalization, unavailable topology, and backoff claims with exact tests and latest task commands | Ten dedicated tests plus adapted inline fixtures match the declared coverage; the fresh artifact is task-scoped, x509-enabled, successful, and contains no prohibited cyfs-p2p-test step | pass |

## Result Summary
- Overall result: accepted
- Outcome: P-001 is satisfied across cleanup, read-side pruning, registration topology, stale failure, and successful live-direct finalization; all prior acceptance findings are closed
- Blocking issues: none found in the current requirement, design, implementation, or validation surfaces
- Next action: parent orchestrator may record accepted final acceptance and complete the auto-pipeline exit conditions and task closeout

## Conclusion
- Accepted / rejected / needs changes: accepted
- Reason: fresh iteration-three counterexample search found atomic topology-consistent state ownership, generation-safe failure handling, validated successful finalization, direct regression coverage for every prior defect, and successful current task-scoped evidence with no remaining blocking issue
